# Integration tests (onlycmd-plugin)
# Copy to .env.local and fill in. Tests are skipped when unset.
GITHUB_TOKEN_TEST=
INTEGRATION_TEST_REPO=

# Optional: app / canary release (e.g. README examples, GitHub Actions)
# GITHUB_TOKEN=
# GH_PAT=
# NPM_TOKEN=
